apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pv-claim
  namespace: pv
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""  # Important: empty string to use manual PV
  volumeName: nfs-slask-pv  # Bind to your existing PV
  resources:
    requests:
      storage: 100Gi  # Less than PV capacity
